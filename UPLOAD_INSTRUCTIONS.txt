WATTZAN MOBILE PORTRAIT FIX V2
=============================

This fixes:
- Navigation drawer being covered by the backdrop on phones
- Full-screen blur when the drawer opens
- Portrait spacing and panel/card boundaries
- Mobile form/control overflow
- Map/chart/table fitting on narrow portrait displays

UPLOAD / REPLACE THESE EXACT PATHS:

1. public/mobile.css                  REPLACE
2. public/dependency-loader.js        REPLACE
3. frontend/mobile.css                REPLACE
4. frontend/dependency-loader.js      REPLACE

No app.js, index.html, styles.css, backend, API, model, or forecasting logic is changed.

The loader now requests:
  /mobile.css?v=16.3.1-mobile-2
to avoid a phone/browser serving the previous cached mobile CSS.

Desktop behavior:
- mobile.css is still skipped on desktop-sized viewports.
- Existing desktop styles remain unchanged.
